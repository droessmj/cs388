#!/bin/bash

for arg in $*
  do
	if [ "$arg" = "sum" ]
		then
			
	fi
	
	if [ "$arg" = "average" ]
		then
			
	fi
	
	if [ "$arg" = "median" ]
		then
			
	fi

	if [ "$arg" = "mode" ]
		then
			
	fi

	if [ "$arg" = "min" ]
		then
			
	fi

	if [ "$arg" = "max" ]
		then
			
	fi

done


